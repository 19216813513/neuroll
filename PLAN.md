# neuroll — 脳トレ Web アプリ 計画書

作成日: 2026-08-04
ステータス: 計画（実装未着手）

---

## 0. このドキュメントの目的

「自分の脳力を測定し、向上させること」に全振りした Web アプリを作る。
ゲーム性・SNS・課金・派手な演出は全部捨てる。残すのは **正確な計測・細かい設定・即リトライ・自己ベスト更新** の 4 つだけ。

---

## 1. プロダクト方針

### 1.1 コアバリュー

| 原則 | 意味 | 具体的な帰結 |
|---|---|---|
| 計測が主役 | 「楽しい」より「正しく測れる」 | `performance.now()` 基準、レイアウトシフト禁止、フレーム落ち禁止 |
| 摩擦ゼロ | 思い立った瞬間に始まり、終わった瞬間に次が始まる | リトライ操作 → 最初の刺激まで 500ms 以内 |
| 自分との勝負 | 他人のランキングは不要 | 全スコアは自分の過去分布に対する相対値で意味づけ |
| 適応的負荷 | 簡単すぎ・難しすぎは訓練にならない | 全種目にステアケース法の自動難度調整を実装 |
| データは自分のもの | ロックインしない | 全履歴を JSON でエクスポート/インポート |

### 1.2 スコープの線引き

**MVP で作らないもの**

- 派手なパーティクル・トランジション・効果音の演出
- 課金、広告
- ネイティブアプリ（PWA でインストール可能にするので不要）
- SNS シェア

**MVP では作らないが、最初から「後で足せる」設計にしておくもの**

| 機能 | MVP | 方針 |
|---|---|---|
| Google SSO ログイン | なし | 認証を**後付けできる層**として分離。Phase 3 で追加（→ §2.5） |
| 端末間同期 | なし | データモデルを最初から同期可能な形にする（→ §9.5） |
| 他人とのランキング | なし | **スコアをサーバーで再検証できる形で記録しておく**（→ §9.4）。これを最初にやらないと、後から過去データが全部使えなくなる |

> ここが今回の改訂の要点。「今は要らないが将来欲しい」機能は、**UI を作らないだけで、
> データの形は最初から将来に合わせておく**。逆に言えば、後から変えられない部分
> （run の ID 体系、スコアの再現性、設定ハッシュの安定性）だけは MVP で正しくやる。

### 1.3 正直な前提（設計に影響するので明記）

認知トレーニングの効果について、研究上ほぼ合意があるのは次の通り:

- **近転移（訓練した課題そのものの成績向上）は明確に起きる。** これは確実に得られる。
- **遠転移（一般知能・日常の認知能力への波及）は議論が続いており、効果があるとしても小さい。**

したがって本アプリは「IQ が上がります」とは謳わない。代わりに、
効果が出る条件として研究上共通して挙げられる 3 点を**設計要件として組み込む**:

1. **適応的難度** — 常に自分の限界付近で訓練する（→ ステアケース実装）
2. **十分な認知的負荷** — 惰性でこなせるモードを作らない（→ lure 試行、干渉条件）
3. **継続** — 短時間・高頻度（→ 1 セッション 3〜5 分、即リトライ、連続日数トラッキング）

---

## 2. プラットフォーム選定 → **Cloudflare**

### 2.1 比較

| | Cloudflare | Google (Firebase) | AWS |
|---|---|---|---|
| 静的ホスティング | Pages: **帯域無制限**、リクエスト無制限、ビルド 500回/月 | Hosting: 10GB 保存 / **転送 360MB/日** | Amplify: 5GB 保存 / 15GB 転送/月 |
| サーバーレス実行 | Workers: 10万req/日 | Cloud Functions: Blaze 必須（要カード） | Lambda: 100万req/月（新Free planは6ヶ月$100クレジット制） |
| DB | D1: 5GB / 読 500万行/日 / 書 10万行/日 | Firestore: 読5万/日・書2万/日・1GB | DynamoDB: 25GB（旧Free Tier） |
| 無料枠超過時 | **停止（課金されない）** | 停止 or Blaze 移行 | **超過分は課金** |
| 請求事故リスク | **ほぼゼロ** | 中（Blaze に上げると青天井） | **高** |
| 本アプリでの月額 | **$0** | $0（ただし転送 360MB/日 の壁） | $0 → 期限切れ後は数ドル |

### 2.2 結論と理由

**Cloudflare Pages（静的配信）+ 将来必要なら Workers + D1。**

- 本アプリは**完全静的 SPA + ローカル保存（IndexedDB）** で成立するため、そもそもサーバー費用が発生しない。
- その上で Cloudflare を選ぶ決め手:
  1. **帯域無制限** — Firebase Hosting の 360MB/日 は、PWA アセット + 音声ファイルを配ると意外に詰む。
  2. **無料枠を超えたら止まる（課金されない）** — AWS の従量課金と違い、寝ている間に請求が跳ねる事故が構造的に起きない。個人プロジェクトではこれが最重要。
  3. **拡張パスが同一プラットフォーム内で $0 のまま伸びる** — 後から端末間同期やランキングを足すとき、Workers + D1 が同じダッシュボード・同じ `wrangler` CLI で完結する。AWS/GCP だと同等構成で有料ラインに触れる。
  4. 独自ドメイン不要なら `neuroll.pages.dev` が無料で付く。使うなら Cloudflare Registrar が原価提供（.com で年 $10 前後）。

**総コスト見積: 月 $0 / 年 $0（独自ドメインを使う場合のみ年 $10 程度）**

### 2.3 デプロイ構成

```
GitHub リポジトリ (main)
        │  push
        ▼
Cloudflare Pages（自動ビルド: npm run build → dist/）
        │
        ▼
   *.pages.dev  ← 全世界 CDN、無制限帯域
```

- プレビューデプロイ: PR ごとに一意 URL が自動発行される（無料）
- ロールバック: ダッシュボードから過去ビルドにワンクリック

### 2.4 公開範囲とクローリング対策

方針: **URL は公開だが、検索エンジンには一切載せない。** 多層で防ぐ。

| 層 | 手段 | 効果 |
|---|---|---|
| 1 | `public/robots.txt` に `User-agent: *` / `Disallow: /` | 行儀の良いクローラを排除。ただし**強制力はない**（お願いベース） |
| 2 | `<meta name="robots" content="noindex, nofollow">` | HTML を読んだクローラのインデックス登録を拒否 |
| 3 | **`public/_headers` で `X-Robots-Tag: noindex, nofollow`** | ← **これが本命**。HTTP ヘッダなので JS 実行前に効き、robots.txt 自体や JSON/アセットにも効く |
| 4 | どこからもリンクを張らない | 被リンクが無ければ発見経路がほぼ無い |
| 5 | プレビューデプロイも同様に保護 | `*.pages.dev` のプレビュー URL が漏れても同じヘッダが付く |

```
# public/_headers
/*
  X-Robots-Tag: noindex, nofollow, noarchive
  Referrer-Policy: no-referrer
  X-Content-Type-Options: nosniff
```

> 注意: `robots.txt` の `Disallow` と `noindex` は**併用すると噛み合わない**ことがある。
> `Disallow` でクロール自体を禁止すると、クローラが `noindex` を読めない。
> そのため **`X-Robots-Tag` ヘッダ（層 3）を主軸**とし、robots.txt は補助と位置づける。

**さらに強い保護が必要になったら**: Cloudflare Access（Zero Trust）で
サイト全体を Google ログインの背後に置ける。**無料プランで 50 ユーザー / 50 アプリまで**、
アプリ側のコード変更ゼロ。ダッシュボードでポリシー（「このメールアドレスのみ許可」）を
設定するだけで、未認証アクセスは Cloudflare のエッジで弾かれる。

### 2.5 認証（Google SSO）の設計 — 2 つの道

「作るかも」段階なので、**どちらにも進める形で MVP を作る**。認証は独立した層にする。

| | 案 A: Cloudflare Access | 案 B: Google Identity Services を自前実装 |
|---|---|---|
| 実装コスト | **ゼロ**（ダッシュボード設定のみ） | 中（GIS 読み込み + Worker で JWT 検証） |
| 何が守れるか | **サイト全体へのアクセス** | API エンドポイント単位 |
| ユーザー識別 | `Cf-Access-Jwt-Assertion` ヘッダから取得 | ID トークンの `sub` |
| PWA オフライン | 認証済みなら Service Worker のキャッシュで動く | 影響なし |
| 他人ランキング | **不向き**（招待した人しか入れない＝閉じたサイト） | **こちら必須** |
| 費用 | 無料（〜50ユーザー） | 無料（Workers 10万req/日 の枠内） |

**推奨する進み方**:

1. **MVP〜Phase 2**: 認証なし。`X-Robots-Tag` のみで運用（§2.4）
2. **自分専用にしたくなったら**: 案 A を有効化。**コード変更ゼロで即座に Google ログイン化できる**
3. **他人とのランキングを始めるとき**: 案 B に移行。Worker + D1 を追加

そのため MVP のコードでは、以下だけ守っておく:

```ts
// src/auth/identity.ts  ← MVP ではこれだけ。中身は最小
export type Identity = { userId: string; source: 'local' | 'google' };

// MVP: 初回起動時に生成した ULID を localStorage に保存して返すだけ
export function getIdentity(): Identity;
```

全ての `Run` に `userId` を持たせておけば、後から Google の `sub` に紐づけ直すだけで
過去データを引き継げる。**逆に userId を持たせずに始めると、後から誰のデータか分からなくなる。**

---

## 3. 技術スタック

### 3.1 選定

| 領域 | 採用 | 理由 |
|---|---|---|
| ビルド | **Vite** | 設定ほぼ不要、HMR 高速、出力が素直 |
| 言語 | **TypeScript** | 種目ごとの設定スキーマが複雑なので型が効く |
| UI | **Preact + preact/compat** | React と同じ書き味で **~4KB**。反応時間計測アプリでランタイム軽量は正義 |
| 状態管理 | **Zustand**（~1KB） | Redux は過剰、Context は再描画が読めない |
| スタイル | **素の CSS + CSS Variables** | Tailwind もビルド1行だが、画面数が少なくテーマ切替が主用途なので変数で足りる。ツールチェーンを1つ減らす |
| ルーティング | **なし**（画面ステートマシン） | 画面が10個未満。`history.pushState` を薄くラップするだけ |
| 永続化 | **IndexedDB**（`idb` ラッパー ~1KB）+ localStorage（設定のみ） | 数千件の試行ログを扱うので localStorage では容量不足 |
| グラフ | **自前 SVG**（依存ゼロ） | 折れ線と棒だけ。Chart.js は 60KB、要らない |
| 音声 | **Web Audio API**（AudioBuffer 事前デコード） | `<audio>` は再生遅延が読めない。N-back の聴覚刺激には不適 |
| PWA | **vite-plugin-pwa** | オフライン動作・ホーム画面追加。飛行機でも訓練できる |
| テスト | **Vitest** | スコア計算・適応アルゴリズム・刺激生成をユニットテスト |
| Lint/Format | **Biome** | ESLint+Prettier より速く、設定1ファイル |

**想定バンドルサイズ: 初期ロード 60〜80KB (gzip)**

### 3.2 あえて使わないもの

- Next.js / Remix — SSR 不要、むしろ初期 JS が増える
- Tailwind — 本アプリの CSS 量なら変数で十分
- アニメーションライブラリ（Framer Motion 等）— 演出不要、かつ計測ノイズ源
- Web フォント — `system-ui` を使う。フォント読み込みによる FOUT/レイアウトシフトは計測の敵
- 状態同期ライブラリ（TanStack Query 等）— サーバーが無い

### 3.3 パフォーマンス予算（CI で計測して逸脱したら失敗にする）

| 指標 | 目標 |
|---|---|
| 初期 JS (gzip) | < 100KB |
| Time to Interactive (4G) | < 1.0s |
| 入力イベント → 画面反映 | < 1 フレーム (16.7ms) |
| セッション中の長タスク (>50ms) | **0 件** |
| Cumulative Layout Shift | **0** |
| 訓練中の GC 起因フレーム落ち | 0（試行中はアロケーションしない） |

---

## 4. 計測アーキテクチャ（本アプリの心臓部）

反応時間を扱う以上、ここを雑にすると全データが無価値になる。

### 4.1 ルール

1. **時刻は必ず `performance.now()`。** `Date.now()` は禁止（NTP 補正で飛ぶ）。
2. **入力時刻はイベントの `event.timeStamp` を使う。** ハンドラ内で `performance.now()` を呼ぶと、
   JS のイベントループ待ち時間が反応時間に混入する。`timeStamp` は同一時間軸なので直接引き算できる。
3. **刺激の「提示時刻」は描画確定後に確定する。**
   `requestAnimationFrame` コールバック内で DOM を変更し、その **次の** rAF の
   コールバック引数（= 直前フレームの表示タイムスタンプ）を提示時刻として採用する。
   さらに対応ブラウザでは `requestPostAnimationFrame` / `element.getAnimations()` を検討。
4. **スケジューリングは rAF ベースの固定タイムステップ。** `setInterval`/`setTimeout` は
   バックグラウンドタブでスロットリングされるため単独では使わない。
5. **`document.visibilitychange` でセッションを即座に無効化する。**
   タブが隠れた試行のデータは捨てる（フラグを立てて記録し、集計から除外）。
6. **試行ループ中はメモリ確保をしない。** 刺激列・入力バッファはセッション開始前に
   固定長配列で確保しておく（GC 由来のフレーム落ち防止）。
7. **ウォームアップ試行**（記録対象外、既定 3 試行）を各セッション先頭に置き、
   JIT・キャッシュ・ユーザーの立ち上がりを吸収する。設定でオフにできる。

### 4.2 環境プロファイルの記録

反応時間の**絶対値**は、ディスプレイのリフレッシュレート・入力デバイス・ブラウザで簡単に 20〜50ms ずれる。
そのため全 run に環境情報を紐づけ、**異なる環境の記録は自己ベスト比較で警告を出す**。

```ts
type DeviceProfile = {
  refreshRateHz: number;      // rAF 間隔を 120 フレーム計測して推定
  inputType: 'keyboard' | 'mouse' | 'touch';
  ua: string;                 // navigator.userAgentData 優先
  screen: { w: number; h: number; dpr: number };
  profileHash: string;        // 上記から導出。比較可否の判定キー
};
```

初回起動時に **リフレッシュレート計測 + 単純反応時間ベースライン（30 試行）** を取り、
以降の RT 系スコアからシステム遅延分を差し引いた「補正 RT」も併記する。

---

## 5. 種目（エクササイズ）

### 5.1 全体像と認知ドメイン対応

| # | 種目 | 主な認知ドメイン | フェーズ |
|---|---|---|---|
| 1 | **N-back** | ワーキングメモリ更新、注意制御 | **MVP** |
| 2 | **数字探し（シュルテ表）** | 視覚探索、周辺視、処理速度 | **MVP** |
| 3 | **タイピング** | 運動系列学習、処理速度、視覚→運動変換 | **MVP** |
| 4 | 暗算 / フラッシュ暗算 | 数的処理、WM 保持 | P2 |
| 5 | ストループ課題 | 抑制制御、干渉解決 | P2 |
| 6 | 数唱（順唱・逆唱） | 言語性 WM スパン | P2 |
| 7 | コルシブロック | 視空間 WM スパン | P2 |
| 8 | Go/No-Go | 反応抑制、衝動性 | P3 |
| 9 | 課題切替（Task Switching） | 認知的柔軟性、切替コスト | P3 |
| 10 | フランカー課題 | 選択的注意 | P3 |
| 11 | Trail Making A/B | 実行機能、セット切替 | P3 |
| 12 | 心的回転 | 視空間処理 | P3 |
| 13 | 変化検出（K 値測定） | 視覚 WM 容量 | P3 |
| 14 | 瞬間数字記憶（チンプテスト） | 瞬間符号化 | P3 |
| 15 | 単純／選択反応時間 | ベースライン測定・体調チェック | **MVP（測定用）** |

> #15 は訓練種目ではなく、**その日の体調・覚醒度の基準線**として毎セッション冒頭に 20〜30 秒だけ走らせる。
> 「今日はスコアが悪い」が実力低下なのか寝不足なのかを切り分けるために必須。

### 5.2 種目詳細 — N-back（最優先）

#### 概要
連続提示される刺激列に対し、「今の刺激は N 個前と同じか？」を判定し続ける。
ワーキングメモリの**更新**機能に最も強い負荷をかける課題。

#### モード
- **シングル N-back**: 位置 / 音（文字・数字）/ 色 / 図形 / 数字 のいずれか 1 モダリティ
- **デュアル N-back**: 位置 + 音（標準構成）。任意の 2 モダリティ組み合わせ可
- **トリプル N-back**: 位置 + 音 + 色（上級者向け）

#### 設定項目（すべて調整可能）

| 設定 | 範囲 / 選択肢 | 既定 |
|---|---|---|
| モダリティ | 位置/音声/色/図形/数字 の複数選択 | 位置+音声 |
| N | 1 〜 9 | 2 |
| 試行数 | 20 + N 〜 100 | 20 + N |
| 刺激提示時間 | 200 〜 3000ms（50ms 刻み） | 500ms |
| 刺激間間隔 (ISI) | 300 〜 5000ms（50ms 刻み） | 2500ms |
| グリッド | 3×3 / 4×4 / 5×5 | 3×3 |
| ターゲット率 | 10 〜 50% | 25% |
| **Lure 試行率** | 0 〜 40% | 0% |
| 反応受付窓 | 提示開始〜次刺激 / 提示中のみ / 任意 ms | 次刺激まで |
| 入力キー | 位置=`A` 音=`L`（変更可）、同時押し可 | A / L |
| フィードバック | 即時 / なし / セッション終了時のみ | 即時（薄い枠色） |
| 適応モード | ON/OFF、閾値カスタム | ON |
| 中央固視点 | 表示 / 非表示 | 表示 |
| 音声セット | 英字 / 数字 / 日本語かな / カスタム | 英字 |
| 音量 | 0 〜 100% | 70% |
| ミス時の音 | あり / なし | なし |

> **Lure 試行**（N±1 の位置に一致がある引っかけ）は、
> 「なんとなくの馴染み感」で答えるのを封じ、実際の WM 更新を強制する。
> 難度が体感で 1〜1.5N 上がる。上級者はこれを 20〜30% に設定するのを推奨。

#### 適応アルゴリズム
Jaeggi 系の標準ルール（ブロック単位）:
```
1 ブロック終了時:
  総エラー数 (miss + false alarm) <= 2  →  N += 1
  総エラー数 >= 5                       →  N -= 1（最小 1）
  それ以外                              →  N 維持
```
オプションで **d′ ベース**（`d′ >= 2.0 で上げ / d′ <= 1.0 で下げ`）も選択可。

#### 指標
- **d′（感度）**= `Z(ヒット率) − Z(誤警報率)` — 主指標。ヒット率単体は「常に押す」で稼げるので不可
- 反応バイアス β / c
- ヒット率、誤警報率、ミス率
- 平均反応時間（正答試行のみ）、RT の標準偏差
- **到達 N**（適応モード時の後半 50% ブロックにおける平均 N）← 長期進捗の主指標
- モダリティ別の内訳（デュアル時: 位置 d′ と 音 d′ を分離）

---

### 5.3 種目詳細 — 数字探し（シュルテ表）

#### 概要
格子状にランダム配置された数字を、順番にできるだけ速くタップ/クリックする。
視覚探索速度と、中央を見たまま周辺視で拾う能力を鍛える。

#### モード
- **昇順**: 1 → 25
- **降順**: 25 → 1
- **交互（往復）**: 1, 25, 2, 24, 3, 23 …（WM 負荷が加わり難度が跳ね上がる）
- **ゴルボフ＝シュルテ（赤黒）**: 赤の昇順と黒の降順を交互に追う。最難関
- **記号混在**: 数字とひらがな/アルファベットを交互に（1, あ, 2, い …）

#### 設定項目

| 設定 | 範囲 / 選択肢 | 既定 |
|---|---|---|
| グリッドサイズ | 3×3 〜 9×9、矩形（4×6 など）も可 | 5×5 |
| モード | 昇順/降順/交互/赤黒/記号混在 | 昇順 |
| 記号種別 | 数字 / ひらがな / アルファベット / 混在 | 数字 |
| **中央固視強制** | OFF / 中央注視ガイド表示 / 視線外れ警告（*） | OFF |
| 誤タップ時の挙動 | 無視 / ペナルティ +1秒 / 進行不可 / 即終了 | ペナルティ +0.5秒 |
| 配置の再シャッフル | なし / 1手ごとに全再配置（超高難度） | なし |
| セルサイズ | 小 / 中 / 大 / px 指定 | 中 |
| セル間隔 | 0 〜 24px | 8px |
| フォントサイズ | 12 〜 64px | 24px |
| 配色 | ダーク / ライト / 高コントラスト / 反転 | ダーク |
| 入力方式 | クリック/タップ / キーボード（テンキー） | クリック |
| 制限時間 | なし / 30/60/120秒（タイムアタック: 制限内に何個） | なし |
| 次ターゲット表示 | 表示 / 非表示 | 表示 |

（*）視線トラッキングは実装しない（カメラ許可が重い）。代わりに中央にターゲット表示を置き、
そこを見ざるを得ない配置にすることで実質的に周辺視を強制する。

#### 指標
- **総所要時間**（主指標）
- 1 手あたり平均時間
- **手あたり時間の標準偏差**（＝安定性。上級者ほど小さくなる）
- 誤タップ回数と誤タップ位置の傾向（グリッド上のヒートマップで表示 → 自分の視野の弱い象限がわかる）
- 前半/後半の速度差（集中の持続）

---

### 5.4 種目詳細 — タイピング

#### 概要
表示された文字列を正確かつ高速に打鍵する。処理速度と運動系列学習の訓練。

#### 素材モード
- **英単語ランダム**（頻出 200 / 1000 / 10000 語から選択）
- **ローマ字日本語**（IME オフ前提でローマ字列を直接判定 — MVP はこれ）
- **数字列**（テンキー訓練）
- **記号・プログラミング**（`{}()[];=>` 等、コード頻出文字）
- **弱点重点ドリル**（過去ログから誤打率・打鍵間隔の悪い文字を抽出し、その文字を含む語だけで出題）← 本アプリの目玉

> **日本語 IME 入力（かな漢字変換込み）は MVP から除外する。**
> `compositionstart/update/end` の挙動が OS/IME 実装で大きく割れ、正確な打鍵計測ができないため。
> ローマ字直接判定（IME オフ）なら keydown 単位で完全に計測できる。IME 対応は P3 で再検討。

#### 設定項目

| 設定 | 範囲 / 選択肢 | 既定 |
|---|---|---|
| 素材 | 英単語/ローマ字/数字/記号/弱点重点 | 英単語 |
| 語彙レベル | 頻出 200 / 1000 / 10000 | 1000 |
| 終了条件 | 時間制（15/30/60/120秒）/ 語数制（10〜100語） | 60秒 |
| ミス時の挙動 | strict（正解まで進めない）/ free（進む）/ 訂正不可 | free |
| バックスペース | 許可 / 禁止 | 許可 |
| 大文字 | あり / なし | なし |
| 句読点・記号 | あり / なし | なし |
| 表示行数 | 1 / 2 / 3 行 | 3 行 |
| キャレット形状 | 下線 / ブロック / 縦棒 | ブロック |
| キャレット追従 | スムーズ / 即時 | 即時（描画負荷ゼロ） |
| ミス音 | あり / なし | なし |
| キーボード表示 | あり（押鍵ハイライト）/ なし | なし |
| レイアウト | US / JIS | JIS |
| フォント | 等幅 / 可変 | 等幅（system-ui monospace） |

#### 指標
- **WPM (net)** = `(正しく打てた文字数 / 5) / 経過分` ← 主指標
- WPM (gross)、KPM（1分あたり打鍵数）
- 正確率（%）、未訂正エラー数
- **IKI（打鍵間隔）分布** — 平均・中央値・p95。p95 が大きい＝特定のキーで詰まっている
- **文字別・バイグラム別エラー率とIKI**（→ 弱点重点ドリルの入力）
- 一貫性スコア（IKI の変動係数）

---

### 5.5 追加種目のアイデア（提案）

要件に無い分の提案。優先度順に、なぜ入れる価値があるかを添える。

| 種目 | 内容 | 入れる価値 |
|---|---|---|
| **ストループ課題** | 「あか」と青文字で書かれた語に対し、文字色を答える | N-back（更新）と別系統の実行機能（**抑制**）を測れる。実装が非常に軽く、設定項目も豊富にできる（一致率、SOA、文字/色の逆マッピング） |
| **暗算 / フラッシュ暗算** | 加減乗除、桁数・項数・提示速度を設定。フラッシュ暗算は数字を高速連続提示して合計 | 実用性が最も高い。設定の自由度（桁数×項数×演算×提示時間）が巨大で「細かく設定したい」要件に完璧に合う |
| **数唱（順唱・逆唱）** | 数字列を提示 → 同順/逆順で再生。正解で桁数+1 | **スパン測定**なのでスコアが整数で明快、自己ベストが分かりやすい。逆唱は WM 操作の純粋な指標 |
| **コルシブロック** | 光ったブロックを同順/逆順でタップ。スパンを伸ばす | 数唱の視空間版。言語 WM と視空間 WM を分けて追跡できる |
| **課題切替** | 「数字なら奇偶」「文字なら母音子音」を交互 or ランダムに切替 | 切替コスト（switch − repeat の RT 差）という、他種目で測れない固有の指標が取れる |
| **Go/No-Go** | Go 刺激に反応、No-Go 刺激では反応を止める | コミッションエラー率が衝動性の指標。1 セッション 90 秒で終わり、疲れているときの穴埋めに最適 |
| **フランカー課題** | `<<><<` の中央矢印の向きを答える | 選択的注意。実装は最も簡単（1日で作れる） |
| **Trail Making A/B** | 1→2→3… (A) / 1→あ→2→い… (B) を線で結ぶ | シュルテ表の親戚だが B のセット切替負荷が独自。A と B の差分が実行機能の指標 |
| **心的回転** | 回転した 3D ブロック図形が同一か鏡像か判定 | 視空間処理。ただし刺激素材の用意（3D レンダリング or 事前生成画像）が必要でコスト高 → P3 |
| **変化検出（K 値）** | 色パッチ群を短時間提示 → 変化の有無を判定。Cowan の K 値を算出 | 視覚 WM の**容量**が数値（通常 3〜4 個）で出る。「自分の脳のスペック」が最も直接的に可視化される |
| **瞬間数字記憶** | 数字が一瞬表示され、消えた後に位置を順に answer | 短時間で強い達成感。継続動機として有効 |

**推奨採用順**: ストループ → 暗算 → 数唱 → コルシブロック → Go/No-Go → 課題切替 → フランカー → TMT → 変化検出 → 心的回転 → 瞬間記憶

---

## 6. 設定システムの設計

「細かく設定できる」を実現しつつ、選択肢の海で溺れないようにする。

### 6.1 3 層構造

```
┌─ プリセット（1クリック）───────────────────────
│  「初級」「標準」「高負荷」「テスト用（固定難度）」
│   + ユーザー定義プリセット（名前を付けて保存、いくつでも）
├─ 基本設定（常時表示）─────────────────────────
│   N、試行数、グリッドサイズ など 3〜5 項目
└─ 詳細設定（折りたたみ）───────────────────────
    ISI、lure率、反応窓、フィードバック方式 など全項目
```

- 起動時は常に **その種目の最後に使った設定** を復元する（リトライ摩擦をゼロにするため）
- 各設定項目に「これを変えると何が起きるか」の 1 行説明をツールチップで付ける
- 設定はスキーマ駆動で定義し、UI は自動生成する:

```ts
type SettingDef =
  | { kind: 'int';    key: string; min: number; max: number; step: number; default: number }
  | { kind: 'ms';     key: string; min: number; max: number; step: number; default: number }
  | { kind: 'enum';   key: string; options: {value: string; label: string}[]; default: string }
  | { kind: 'multi';  key: string; options: {value: string; label: string}[]; default: string[] }
  | { kind: 'bool';   key: string; default: boolean }
  | { kind: 'key';    key: string; default: string };   // キーバインド

type ExerciseDef = {
  id: string;
  name: string;
  domains: CognitiveDomain[];
  settings: SettingDef[];
  scoreKeys: string[];              // どの指標が「スコア」か
  primaryScoreKey: string;          // 自己ベスト判定に使う指標
  higherIsBetter: boolean;
  comparabilityKeys: string[];      // ★ ハイスコア比較に影響する設定キー
  run: (cfg, ctx) => Promise<RunResult>;
};
```

`comparabilityKeys` により、種目定義から自動でスコア比較の粒度が決まる（次節）。

### 6.2 設定の変更が「難度」に効くか「見た目」に効くか

これを区別しないとハイスコアが破綻する。各設定にフラグを持たせる:

- **difficulty 設定**（N、ISI、グリッドサイズ、lure率…）→ 変えると**別のハイスコア枠**になる
- **cosmetic 設定**（配色、フォントサイズ、音量、キーバインド…）→ ハイスコア枠は共有

---

## 7. ハイスコアとスコアリング

### 7.1 スコアの比較単位（バケット）

```
scoreBucket = hash(exerciseId + 正規化した difficulty 設定 + deviceClass)
```

- 難度設定が 1 つでも違えば別枠。「N=2 の自己ベスト」と「N=3 の自己ベスト」は別物
- 反応時間が主指標の種目のみ `deviceClass`（desktop-keyboard / mobile-touch など粗い分類）を含める
- ハッシュは**設定値を正規化してからキーをソートし、JSON 化して SHA-256**。
  こうしないと同じ設定でもキー順で別ハッシュになる。将来サーバーで照合するので**安定性が必須**

```ts
function bucketOf(exerciseId: string, cfg: Config, def: ExerciseDef, dev: DeviceProfile) {
  const diff = def.settings
    .filter(s => s.affects === 'difficulty')      // cosmetic は除外
    .map(s => [s.key, normalize(s, cfg[s.key])] as const)
    .sort(([a], [b]) => a < b ? -1 : 1);          // キー順を固定
  return sha256(JSON.stringify({ e: exerciseId, v: def.bucketVersion, d: diff, dc: dev.deviceClass }));
}
```

`bucketVersion` は種目の**仕様を変更したとき**（刺激生成アルゴリズムを直した等）に上げる。
過去スコアと混ざらないようにするための安全弁。

### 7.2 ★ ハイスコア画面 — 設定条件で表示を切り替える

これが本アプリのハイスコアの中核。**「どの設定で測ったスコアか」を自在に絞り込める**。

#### ビュー 1: 条件フィルタ + ランキング表

```
┌─ N-back ─────────────────────────────────────────────┐
│ 絞り込み:                                              │
│   モダリティ [位置+音 ▾]  N [すべて ▾]  ISI [2500ms ▾] │
│   試行数 [20+N ▾]  lure率 [0% ▾]  端末 [PC ▾]         │
│   期間 [全期間 ▾]     □ 無効な記録も含める              │
│   [設定を1つだけ自由軸にする: N ▾]                     │
├──────────────────────────────────────────────────────┤
│  #   d′     N   日付           条件                    │
│  1  2.84    3   2026-07-28    ISI2500 / lure0%   ★PB  │
│  2  2.71    3   2026-08-01    ISI2500 / lure0%        │
│  3  2.66    3   2026-07-19    ISI2500 / lure0%        │
│  …                                                    │
└──────────────────────────────────────────────────────┘
```

- 各設定項目が**独立したフィルタ**になる（種目定義の `SettingDef` から UI を自動生成）
- 既定値は「**今の設定と完全一致**」。ワンクリックで各項目を「すべて」に広げられる
- **「自由軸」指定**: 1 項目だけ「すべて」にすると、その軸方向の自己ベスト一覧が並ぶ
  （例: N を自由軸 → N=1〜5 それぞれの PB が一覧で見える）

#### ビュー 2: ピボット表（2 軸マトリクス）

設定項目を 2 つ選んで縦横に取り、交点に PB を出す。**自分の実力の地形図**になる。

```
      ISI→   1500ms  2000ms  2500ms  3000ms
  N=1         3.12    3.30    3.41    3.45
  N=2         2.20    2.55    2.84    2.91
  N=3         1.05    1.48    1.77    1.92
  N=4          --     0.42    0.88    1.10     ← 未挑戦セルはグレー
```

- セルの背景を値に応じて薄く着色（ヒートマップ）
- **未挑戦のセルをクリックするとその設定で即セッション開始** → 空白を埋めたくなる導線
- 試行回数が 3 回未満のセルは「暫定」マークを付ける

#### ビュー 3: バケット一覧（全種目横断）

自分が今までに作った全バケットを、記録回数・最終実施日・PB とともに一覧表示。

- 「30日以上触っていないバケット」を上に出す → 訓練の偏りが見える
- バケットの**ピン留め**: よく使う 3〜5 条件をホーム画面に常時表示
- バケットの**アーカイブ**: 試しただけの条件を一覧から隠す（データは消さない）

#### 「近い設定」の扱い

厳密一致だと、ISI を 2500 → 2400 に変えただけで PB がリセットされて心が折れる。
そこで **参考比較モード** を用意する:

- 各 difficulty 設定に `tolerance`（比較許容幅）を定義する
  例: ISI ±10%、試行数 ±20%、提示時間 ±10%
- 厳密一致のバケットが 3 件未満のとき、許容内の記録を**薄いグレーで参考表示**する
- 参考記録は PB 判定には**使わない**（ここを混ぜると数字の意味が壊れる）

### 7.3 表示する 5 つの数字

### 7.2 表示する 5 つの数字

各種目・各バケットについて:

1. **PB（自己ベスト）** — 全期間の最高値
2. **直近 5 回平均** — 「今の実力」。単発のまぐれを排除
3. **本日ベスト**
4. **z スコア** — 自分の過去全記録の分布上でこの回はどこか（`(x − μ) / σ`）
5. **トレンド** — 直近 10 回の線形回帰の傾き（↗ / → / ↘）

> **PB だけを見せると、たまたま出た 1 回に縛られて心が折れる。**
> 「直近 5 回平均」を PB と同じ大きさで併記するのが本アプリの立場。

### 7.4 統合指標「neuroll スコア」

種目横断の総合値。単位の違う指標（d′、秒、WPM）を自分の過去分布で標準化して合成する:

```
種目スコア = 100 + 15 × z（その種目における自分の全履歴に対する z 値）
             ※ 直近 5 回の中央値を x として算出
             ※ 履歴 10 回未満の種目は算出しない（"測定中"表示）

neuroll スコア = ドメイン別に平均 → ドメイン間で平均
  ドメイン: ワーキングメモリ / 処理速度 / 抑制制御 / 認知的柔軟性 / 視空間
```

- IQ と同じ 100±15 スケールにするのは、**自分の過去の自分が 100** という意味づけのため
- ドメイン別に平均してから合成するので、N-back ばかりやっても総合は上がらない → 満遍なく訓練する動機になる
- レーダーチャート（自前 SVG）で 5 ドメインを表示。凹んでいるドメインを提案してくる

### 7.5 記録更新時の演出

「複雑なエフェクトは不要」との要件に従い、**PB 更新時は数値の色が変わり、`NEW PB` の 3 文字が出るだけ**。
アニメーションなし、音なし（音は設定で ON にできる）。0.1 秒でも早く次に行きたいので。

---

## 8. 即リトライの設計

**目標: 結果画面で `R` または `Space` を押してから、最初の刺激が提示されるまで 500ms 以内。**

### 8.1 実現手段

| 手段 | 内容 |
|---|---|
| 事前生成 | **結果画面を表示している間に、次セッションの刺激列を生成して保持しておく**。リトライ時は生成コストゼロ |
| 音声プリデコード | 全 AudioBuffer をアプリ起動時に一括デコード。セッション開始時のデコード待ちを排除 |
| 設定保持 | リトライは常に同一設定。設定画面を経由しない |
| 確認なし | 「本当にやり直しますか？」を出さない。中断も即座 |
| DOM 再利用 | セッション用 DOM はマウントしたまま状態だけリセット。再マウントしない |
| カウントダウン | 既定 **0.8 秒**（`3,2,1` ではなく短いバー）。設定で 0〜3 秒に変更可、0 も許可 |
| 結果画面の即応 | 結果は計算済みの値を表示するだけ。DB 書き込みは非同期で裏に流す |

### 8.2 グローバルキーバインド

| キー | 動作 |
|---|---|
| `Space` / `R` | リトライ（同一設定） |
| `Enter` | 次の推奨種目へ |
| `Esc` | セッション中断 → 即結果 or 即リトライ選択 |
| `S` | 設定を開く |
| `Tab` | 種目切替（ホーム） |
| `1`〜`9` | ホームで種目直接起動 |
| `G` | 進捗グラフ |
| `?` | キーバインド一覧 |

全キーバインドは設定で変更可能。

### 8.3 セッション長の既定

「短く・高頻度」を守るため、既定は **1 セッション 2〜4 分**。
最長でも設定 UI 上で「10 分以上」を選ぶと警告を出す（集中が保てず、データがノイズまみれになるため）。

---

## 9. データモデル

### 9.1 IndexedDB スキーマ

```ts
// DB: "neuroll", version: 1

// store: runs           key: id (ULID)
type Run = {
  id: string;                  // ULID: 時刻順にソート可能 + 端末をまたいで衝突しない
  schemaVersion: number;       // マイグレーション用
  userId: string;              // ★ MVP はローカル生成 ULID。後で Google sub に紐づけ替える
  deviceId: string;            // ★ 端末識別。同期時のコンフリクト解決に使う

  exerciseId: string;
  bucketVersion: number;       // 種目仕様のバージョン（§7.1）
  scoreBucket: string;         // §7.1 のハッシュ
  configSnapshot: object;      // 完全な設定のコピー（後から解析・再検証できるように）
  seed: string;                // ★ 刺激列生成に使ったシード。これがあれば刺激列を完全再現できる
  deviceProfile: DeviceProfile;

  startedAt: number;           // epoch ms
  durationMs: number;
  metrics: Record<string, number>;   // d_prime, wpm, totalMs, ...
  primaryScore: number;

  valid: boolean;              // タブ離脱・中断があれば false
  invalidReason?: string;
  suspicion?: string[];        // ★ 妥当性チェックに引っかかった項目（§9.4）

  appVersion: string;
  note?: string;               // ユーザーの自由メモ（"寝不足" 等）

  // ↓ 同期用。MVP では未使用だがフィールドだけ用意する（§9.5）
  updatedAt: number;
  deletedAt?: number;          // 論理削除（物理削除しない = 同期が単純になる）
  syncedAt?: number;
};
// index: by-exercise, by-bucket, by-startedAt, by-day, by-updatedAt

// store: trials         key: [runId, trialIndex]
type Trial = {
  runId: string;
  i: number;
  stimulus: unknown;           // 種目ごとの型
  isTarget: boolean;
  response: unknown | null;
  correct: boolean | null;
  rtMs: number | null;
  presentedAt: number;         // performance.now 基準（run 内相対）
};
// 詳細ログは既定 ON。設定で OFF 可（容量節約）

// store: settings       key: exerciseId
// store: presets        key: id
// store: meta           key: string  (baseline RT, streak, 初回起動日 など)
```

### 9.2 容量見積

1 run ≒ 1KB + trials 100 件 × 60B ≒ 7KB。
1 日 10 run × 365 日 = 約 **25MB/年**。IndexedDB の枠（通常ディスク空き容量の数%）に対して余裕。

### 9.3 バックアップとポータビリティ

- **JSON エクスポート**（全 run + trials + 設定 + プリセット）ワンクリック
- **JSON インポート**（マージ or 置換）
- **CSV エクスポート**（run 単位。Excel/R/Python で自分で解析したい人向け）
- 30 日エクスポートしていないと、ホーム画面に控えめなリマインダーを出す
  （ブラウザのサイト データ削除で全消失するリスクがあるため）

### 9.4 ★ スコアの再検証可能性（将来の他人ランキングへの布石）

**今は他人ランキングを作らない。だが、後から作ったときに「過去の記録も対象にできる」ようにしておく。**
そのために MVP の時点から次を満たす。

#### (a) 刺激列は決定論的に再現できること

```ts
// core/rng.ts — シード付き PRNG（xoshiro128** など。Math.random は使わない）
const rng = createRng(seed);
const stimuli = generateStimuli(config, rng);   // 同じ (seed, config) → 必ず同じ刺激列
```

`seed` を Run に保存しておけば、**サーバーは (seed, config) から刺激列を再構築し、
クライアントが送ってきた応答列と突き合わせてスコアを再計算できる**。
クライアントが申告したスコアを信じる必要がなくなる。

#### (b) スコア計算関数が純粋であること

```ts
score(config, stimuli, responses) -> metrics
```

DOM・時刻・グローバル状態に依存しない純関数にする。同じ関数を Worker 側でそのまま実行できるよう、
`exercises/*/score.ts` と `stats/` は**ブラウザ API を一切参照しない**こと。これは MVP でも守る。

#### (c) 妥当性チェック（自分に対しても有効）

サーバーが無くても、**クライアント側で記録時に走らせて `suspicion[]` に残す**。
自分のデータの品質管理としても意味がある。

| チェック | 判定 |
|---|---|
| RT が生理学的下限（約 100ms）を下回る試行の割合 | 15% 超で疑わしい |
| RT の分散が極端に小さい（機械的な等間隔入力） | 変動係数 < 0.05 で疑わしい |
| セッション実時間 ≒ 刺激列から計算される理論所要時間 | ±5% を超えたら疑わしい |
| タブ非表示時間の合計 | 0 でなければ `valid: false` |
| 応答数 > 刺激数 | 即無効 |

> 正直な注記: **ブラウザで動く以上、本気の不正は防げない**（DevTools で何でもできる）。
> 他人ランキングを作る際は「サーバー再計算 + 妥当性チェック + 統計的外れ値検出」で
> **雑な不正を弾く**までが現実的な到達点。そこは割り切る。
> ただし (a)(b) を最初にやっておかないと、その水準にすら到達できない。

#### (d) 送信するときも、送るのは「素材」だけ

将来ランキングに投稿する際、送信するのは
`{ runId, userId, exerciseId, bucket, config, seed, responses[], startedAt, deviceClass }`。
**metrics（スコア）は送らない、あるいは送っても採用しない。** サーバーが計算した値だけを正とする。

### 9.5 同期レディな設計

MVP は完全ローカルだが、後から同期を足すときに**データ移行を発生させない**形にしておく。

- **runs は追記のみ（append-only）** — 更新も削除もしない。論理削除 `deletedAt` のみ
  → 複数端末のデータは**単純な集合和（union）でマージできる**。競合解決ロジックが不要になる
- **ID は ULID** — 端末をまたいでも衝突せず、生成時刻順にソートできる
- **settings / presets は Last-Write-Wins** — `updatedAt` の新しい方を採用。設定は少量なので単純で良い
- 同期プロトコル（Phase 3）は差分プルのみの単純な形:
  ```
  GET  /sync?since=<updatedAt>   → 自分の全端末の新しい run
  POST /sync                     → ローカルの未同期 run を送信
  ```
- D1 のスキーマは Run 型とほぼ 1:1。5GB / 書き込み 10万行/日 の無料枠に対し、
  1日10 run のペースなら**まったく問題にならない**

---

## 10. 進捗分析画面

数字を見て「効いている」と確認できないと続かない。

- **学習曲線**: 種目×バケットごとの時系列。生データ点 + 移動平均（直近10回）
- **ドメイン別レーダー**: 5 ドメインの現在値と 30 日前との比較（重ね描き）
- **時間帯別パフォーマンス**: 0〜23 時のヒートマップ。**自分の脳のゴールデンタイムがわかる**
- **曜日別 / 睡眠メモとの相関**（メモ機能に紐づけ）
- **セッション内推移**: 1 回目 vs 3 回目 vs 5 回目の平均 → 自分のウォームアップ量がわかる
- **天井検知**: 直近 15 回の回帰傾きが 0 に収束したら「難度を上げる時期です」と提案し、
  推奨設定（N+1 / グリッド拡大 / lure 追加）をワンクリック適用できるボタンを出す
- **連続日数（ストリーク）** と 週あたり訓練日数
- 全グラフは自前 SVG。ホバーで値表示、それ以上の演出なし

---

## 11. 画面構成

```
[ホーム]
 ├ neuroll スコア（大）+ 5ドメインレーダー（小）
 ├ ストリーク / 今日の訓練時間
 ├ 種目タイル（各タイルに PB と直近5回平均を表示）
 └ 「今日の推奨メニュー」← 凹んでいるドメインから 3 種目を自動提案

[種目 > 設定]           ← プリセット / 基本 / 詳細（折りたたみ）
[種目 > セッション]     ← 訓練中。UI は極限までミニマル、余計な要素ゼロ
[種目 > 結果]           ← 指標、PB 比較、z スコア。R で即リトライ
[ハイスコア]            ← ★ §7.2。条件フィルタ / ピボット表 / バケット一覧 の3ビュー
[進捗]                  ← §10
[全体設定]              ← テーマ、キーバインド、音、データ入出力、計測較正、（将来）アカウント
[ベースライン測定]      ← 初回起動時 + 任意実行
```

セッション画面のルール:
- 背景は単色、中央固視点、刺激以外は何も動かない
- 進捗バーは**訓練中に出さない**（気が散る）。設定で ON 可
- スコアは訓練中に**出さない**（既定）。設定で ON 可

---

## 12. アクセシビリティ・UX

- **完全キーボード操作** — マウス無しで全機能に到達できる
- **ダークモード既定**（長時間の目の負担軽減）。ライト / 高コントラストも用意
- **色覚多様性配慮** — 正誤フィードバックを色だけに頼らない（枠線の太さ・形状も変える）。
  ただしストループ課題は色が本質なので、色覚特性に応じた代替版（形状ストループ）を用意
- **音声なしモード** — N-back の聴覚刺激を視覚（画面下部の文字）に置換
- **モーション削減** — `prefers-reduced-motion` を尊重（そもそも動きが少ない）
- **タッチ対応** — シュルテ表・コルシブロックはタップ操作。ただし RT 系種目は
  タッチだと遅延が大きいことを明示し、別バケットで記録
- **文字サイズ** — ルート `font-size` をユーザー設定で 14〜22px に

---

## 13. ディレクトリ構成（案）

```
neuroll/
├─ index.html
├─ vite.config.ts
├─ package.json
├─ biome.json
├─ PLAN.md                      ← 本書
├─ public/
│   ├─ _headers                 ← X-Robots-Tag: noindex（§2.4）★ 最初に置く
│   ├─ robots.txt               ← Disallow: /
│   ├─ manifest.webmanifest
│   ├─ icons/
│   └─ audio/                   ← N-back 用の短い音声（letters/, digits/, kana/）
└─ src/
    ├─ main.tsx
    ├─ app/
    │   ├─ App.tsx
    │   ├─ router.ts             ← 薄い画面ステートマシン
    │   └─ keybindings.ts
    ├─ core/                     ← ★ 種目に依存しない基盤。ここの品質が全て
    │   ├─ clock.ts              ← performance.now ラッパ、rAF スケジューラ
    │   ├─ scheduler.ts          ← 固定タイムステップの刺激提示ループ
    │   ├─ input.ts              ← keydown/pointer の timeStamp 正規化
    │   ├─ rng.ts                ← シード付き乱数（再現可能な刺激列）
    │   ├─ audio.ts              ← Web Audio、事前デコード
    │   ├─ visibility.ts         ← タブ離脱検知 → run 無効化
    │   ├─ deviceProfile.ts      ← リフレッシュレート推定、環境ハッシュ
    │   └─ staircase.ts          ← 適応難度アルゴリズム（共通）
    ├─ stats/
    │   ├─ sdt.ts                ← d′, β, c（信号検出理論）
    │   ├─ descriptive.ts        ← 平均/SD/中央値/p95/変動係数
    │   ├─ zscore.ts
    │   ├─ trend.ts              ← 線形回帰、移動平均、天井検知
    │   └─ composite.ts          ← neuroll スコア合成
    ├─ exercises/
    │   ├─ registry.ts           ← ExerciseDef の一覧
    │   ├─ nback/
    │   │   ├─ def.ts            ← 設定スキーマ・指標定義
    │   │   ├─ generate.ts       ← 刺激列生成（ターゲット率・lure 率を保証）
    │   │   ├─ Session.tsx
    │   │   ├─ score.ts
    │   │   └─ nback.test.ts
    │   ├─ schulte/
    │   ├─ typing/
    │   └─ reactiontime/
    ├─ scores/                   ← ★ バケット・ハイスコア閲覧のロジック
    │   ├─ bucket.ts             ← 設定の正規化 + 安定ハッシュ（§7.1）
    │   ├─ query.ts              ← 条件フィルタ / ピボット集計（§7.2）
    │   └─ validate.ts           ← 妥当性チェック → suspicion[]（§9.4）
    ├─ auth/
    │   └─ identity.ts           ← MVP はローカル ULID を返すだけ。後で Google SSO に差し替え
    ├─ ui/
    │   ├─ SettingsForm.tsx      ← SettingDef からフォームを自動生成
    │   ├─ ResultView.tsx
    │   ├─ HighScores.tsx        ← ★ フィルタ / ピボット / バケット一覧
    │   ├─ charts/               ← Line.tsx, Radar.tsx, Heatmap.tsx（全て素の SVG）
    │   └─ primitives/
    ├─ store/
    │   ├─ db.ts                 ← IndexedDB (idb)
    │   ├─ runs.ts
    │   ├─ settings.ts
    │   ├─ migrate.ts            ← schemaVersion のマイグレーション
    │   └─ backup.ts             ← JSON/CSV 入出力
    └─ styles/
        ├─ tokens.css            ← CSS 変数（色・間隔・タイポ）
        └─ base.css
```

**設計上の要点**: `core/` と `stats/` が種目から完全に独立していること。
新しい種目を足すときに触るのは `exercises/<name>/` と `registry.ts` の 1 行だけ、という状態を維持する。
種目 #4 以降の追加コストがここで決まる。

---

## 14. 開発ロードマップ

### 14.0 「まずローカルで実装すべきか？」→ **はい。ただしデプロイは最初に通す**

推奨は **「Day 1 に空ページをデプロイして経路を通し、以降の開発は 100% ローカル」**。

**ローカル中心にすべき理由**

- 本アプリは**サーバーを一切使わない**（IndexedDB 完結）。ローカルと本番で挙動が変わる要素が無い
- Vite の HMR は変更が 100ms 以内に反映される。デプロイを待つ理由がゼロ
- タイミング計測の検証（フレーム落ち・長タスク）は**ローカルの DevTools でしかまともにできない**
- Cloudflare Pages のビルド回数（500/月）を無駄に消費しない

**それでも Day 1 にデプロイを通しておくべき理由**

- 「動くものを本番で見る」までの経路が塞がっていると、完成後に詰まる（設定ミス、パス問題、`_headers` の効き）
- **`X-Robots-Tag` が実際に効いているかは、デプロイして `curl -I` しないと確認できない**
- PWA / Service Worker は `https` 前提の挙動があり、本番で一度確認しておく必要がある
- スマホ実機での確認（タッチ入力・リフレッシュレート）に本番 URL が要る

```bash
curl -sI https://neuroll.pages.dev | grep -i robots
```

**結論**: フェーズ 0 の初日に「Hello + `_headers`」だけデプロイして経路とヘッダを確認 →
以降は `npm run dev` でローカル開発 → 各フェーズの区切りで push してデプロイ。

### フェーズ 0: 基盤（推定 3〜4 日）
- git init + GitHub リポジトリ作成、Vite + TS + Preact + Biome セットアップ
- **`public/_headers` + `robots.txt` を最初に置き、Cloudflare Pages にデプロイして
  `curl -I` で `X-Robots-Tag` を確認**（§2.4 / §14.0）
- `core/rng.ts` — シード付き決定論的 PRNG（§9.4(a)。後から差し替えると過去データが再現不能になるので最初に確定させる）
- `core/clock.ts` `core/scheduler.ts` `core/input.ts` — **提示時刻と入力時刻の精度検証をここで完了させる**
- `deviceProfile.ts` — リフレッシュレート推定、deviceClass 判定
- `auth/identity.ts` — ローカル ULID 発行（§2.5）
- IndexedDB スキーマ（**Run 型は §9.1 の全フィールドを最初から入れる**）+ runs 保存 + エクスポート
- `scores/bucket.ts` — 安定ハッシュ（§7.1）
- 単純反応時間種目（基盤の動作検証を兼ねた最小の種目）
- CSS トークン、ダークテーマ、キーバインド基盤

> フェーズ 0 の完了条件: **単純反応時間を 100 試行走らせ、RT 分布が 200〜300ms に収まり、
> 長タスク 0 件・フレーム落ち 0 件であること。** ここが通らなければ先に進まない。

### フェーズ 1: MVP（推定 8〜10 日）
- **N-back**（シングル + デュアル、lure、適応、d′）
- **シュルテ表**（昇順/降順/交互、グリッド可変）
- **タイピング**（英単語、時間制、WPM/正確率/IKI）
- 設定 UI 自動生成 + プリセット
- 結果画面 + PB / 直近5回平均 / z スコア
- **ハイスコア画面 ビュー1（条件フィルタ + ランキング表）**（§7.2）
- `scores/validate.ts` — 妥当性チェック（§9.4(c)）
- 即リトライ（500ms 目標の達成を計測して確認）
- 進捗グラフ（学習曲線のみ）
- PWA 化（オフライン動作）

**→ ここでリリース。以降は自分で使いながら改善する。**

### フェーズ 2: 深化（推定 7〜9 日）
- **ハイスコア画面 ビュー2（ピボット表）+ ビュー3（バケット一覧）**（§7.2）
  ※ 未挑戦セルから即セッション開始する導線を含む
- ストループ / 暗算・フラッシュ暗算 / 数唱 / コルシブロック
- neuroll スコア + ドメインレーダー
- 進捗分析の全機能（時間帯ヒートマップ、天井検知、推奨メニュー）
- タイピングの弱点重点ドリル
- トリプル N-back、ゴルボフ＝シュルテ

### フェーズ 3a: アカウント（必要になったら / 推定 0.5〜3 日）

必要度に応じて 2 段階。**どちらも MVP のコードを壊さない。**

- **段階 1（30 分）**: Cloudflare Access を有効化 → サイト全体が Google ログイン必須になる。
  コード変更ゼロ。「自分だけが使える状態にしたい」ならここで完了
- **段階 2（2〜3 日）**: Google Identity Services + Worker で JWT 検証 + D1。
  `auth/identity.ts` の実装を差し替え、既存の `userId` を Google の `sub` に移行するだけ

### フェーズ 3b: 同期・拡張（随時）
- 端末間同期（Workers + D1。§9.5 の差分プル。**この段階でも $0 の想定**）
- Go/No-Go、課題切替、フランカー、TMT、変化検出、心的回転、瞬間記憶
- 詳細解析ビュー（試行レベルの RT 系列、エラー傾向）
- 日本語 IME タイピングの再検討

### フェーズ 4: 他人とのランキング（将来 / 判断は後日）
- Worker でスコア再計算（§9.4(a)(b) の資産をそのまま使う）
- バケット単位の公開リーダーボード（設定条件が同一の記録のみ比較）
- 統計的外れ値検出、投稿レート制限
- **この段階に来ても、フェーズ 0〜1 で作ったデータはそのまま使える**（seed と responses があるため）

---

## 15. テスト方針

| 対象 | 方法 |
|---|---|
| 刺激列生成 | Vitest。ターゲット率・lure 率が設定値に収束するか、1000 回生成して統計的に検証 |
| d′ / z / 統計関数 | Vitest。既知の入出力ペアで検証（境界: ヒット率 100% / 0% の補正） |
| 適応アルゴリズム | Vitest。シミュレートした正答系列を流し、想定通り N が上下するか |
| スコア計算 | Vitest。全種目のゴールデンテスト |
| タイミング精度 | 手動 + 計測スクリプト。**外部の高速度撮影で実測するのが理想**だが、
最低限 rAF ログでフレーム落ち 0 を確認 |
| パフォーマンス予算 | CI で `vite build` 後のバンドルサイズをチェック、閾値超過で失敗 |
| ブラウザ差 | Chrome / Firefox / Safari で N-back 20 試行を手動実行し RT 分布を比較 |

---

## 16. リスクと対応

| リスク | 影響 | 対応 |
|---|---|---|
| ブラウザのタイマー分解能制限（Spectre 対策で `performance.now()` が粗い） | RT 精度低下 | 現行ブラウザは 5μs〜100μs 粒度で、RT 計測（ms オーダー）には十分。起動時に実測して粒度を記録・表示 |
| ディスプレイと入力デバイスの遅延（環境で 20〜50ms 変動） | 絶対値が比較不能 | ベースライン RT を測り差し引く。異なる `deviceProfile` の記録は別バケット。UI で「絶対値でなく自分内の変化を見る」と明示 |
| ブラウザのサイトデータ削除で全履歴消失 | 致命的 | エクスポート導線をホームに常設。30日リマインダー。P3 でクラウド同期 |
| 「効果があるのか」がわからず継続が切れる | 使われなくなる | 学習曲線とストリークを主要導線に。天井検知で難度上げを能動的に提案 |
| 種目が増えるほどコードが肥大 | 保守不能 | `core`/`stats` の種目非依存を厳守。種目追加は 1 ディレクトリ + 1 行の登録のみ |
| 設定項目が多すぎて使われない | 主要要件の空振り | プリセット 4 種を先頭に置き、詳細は折りたたみ。最終設定を必ず復元 |
| バックグラウンドタブでのスロットリング | 不正なデータ混入 | `visibilitychange` で run を `valid: false` にし集計から除外 |
| Cloudflare Pages のビルド上限（500回/月） | デプロイ不可 | 個人開発で月 500 プッシュは非現実的。到達しそうならローカルビルド + `wrangler pages deploy` に切替 |

---

## 17. 意思決定サマリ

| 論点 | 決定 | 一言理由 |
|---|---|---|
| クラウド | **Cloudflare Pages** | 帯域無制限・超過で停止（課金されない）・拡張パスが同一無料枠内 |
| バックエンド | **MVP はなし（ローカル完結）** | サーバー不要で $0 と最速起動。ただしデータの形は将来のサーバーに合わせておく |
| 公開範囲 | **公開 URL + `X-Robots-Tag: noindex`** | クロール対策の本命は HTTP ヘッダ。robots.txt は補助 |
| 認証 | **MVP はなし。必要になったら Cloudflare Access → 自前 SSO の2段階** | 段階1はコード変更ゼロで Google ログイン化できる |
| 開発の進め方 | **Day 1 にデプロイ経路を通し、以降はローカル開発** | サーバーが無いのでローカルと本番の差が無い。ただしヘッダと PWA は本番確認が要る |
| フレームワーク | **Vite + Preact + TS** | 軽量・単純・型が効く。React 資産もそのまま使える |
| スタイル | **素の CSS 変数** | ツールチェーンを 1 つ減らす |
| 永続化 | **IndexedDB + JSON エクスポート** | 試行ログの容量とポータビリティ |
| 必須種目 | N-back / シュルテ表 / タイピング | 要件通り。+ 単純反応時間（体調ベースライン用） |
| 追加提案の最優先 | **ストループ・暗算・数唱** | 別ドメインを安く追加でき、設定の自由度も高い |
| スコア | **バケット別 PB + 直近5回平均 + z スコア + neuroll スコア** | 単発のまぐれに縛られず、種目横断で総合力を見る |
| ハイスコア閲覧 | **設定条件フィルタ + 2軸ピボット表 + バケット一覧** | 「どの設定で測ったか」で自在に絞り込める。ピボット表は自分の実力の地形図になる |
| 他人ランキング | **今は作らない。ただし seed + responses を記録し再検証可能にしておく** | これを最初にやらないと、後から過去データが全部使えなくなる |
| リトライ | **`R`/`Space` で 500ms 以内に再開** | 刺激列の事前生成と音声プリデコードで実現 |
| 演出 | **ほぼゼロ** | 計測ノイズと待ち時間を生むだけ |

---

## 18. デプロイ準備 — そちらで用意していただくもの

現在このディレクトリは**空で、git リポジトリでもない**（確認済み）。以下を揃えれば着手できる。

### 18.1 MVP に必須（これだけあれば動く）

| # | 項目 | 備考 |
|---|---|---|
| 1 | **Node.js LTS（22.x 以降）** | `node -v` で確認。未導入なら [nodejs.org](https://nodejs.org/) から |
| 2 | **GitHub アカウントと空のリポジトリ** | 名前は `neuroll` 想定。**Private で問題ない**（Cloudflare Pages は Private リポジトリも無料で連携可） |
| 3 | **Cloudflare アカウント** | 無料。クレジットカード登録**不要**（Workers 無料枠のみ使う限り） |

### 18.2 Cloudflare 側の設定（ダッシュボードで 5 分）

1. Cloudflare ダッシュボード → **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
2. GitHub を認可し、`neuroll` リポジトリを選択
3. ビルド設定:
   - Framework preset: **None**（Vite を手動指定する方が確実）
   - Build command: `npm run build`
   - Build output directory: `dist`
   - Node version: 環境変数 `NODE_VERSION` = `22`
4. デプロイ → `https://neuroll.pages.dev` が発行される

> **CLI 派の場合**: `npm i -D wrangler` → `npx wrangler login`（ブラウザで認可）→
> `npx wrangler pages deploy dist`。API トークンの手動発行は不要。
> GitHub 連携と CLI は併用できるので、まず GitHub 連携で作っておくのを推奨。

### 18.3 任意（後からいつでも追加できる）

| 項目 | いつ必要か | 費用 |
|---|---|---|
| 独自ドメイン | `*.pages.dev` が嫌になったら | Cloudflare Registrar で原価。`.com` 年 $10 前後 |
| **Cloudflare Access（Google ログインで全体を保護）** | 「自分だけが使える状態」にしたくなったら | **無料（〜50ユーザー）**。ダッシュボードのみ、コード変更ゼロ |
| Google Cloud Console の OAuth クライアント ID | フェーズ 3a 段階2（自前 SSO）に進むとき | 無料 |
| D1 データベース | 端末間同期を始めるとき | 無料枠内 |

### 18.4 デプロイ疎通の確認コマンド

フェーズ 0 の初日、Hello ページをデプロイしたら必ずこれを実行する。

```bash
curl -sI https://neuroll.pages.dev | grep -i -E "x-robots-tag|referrer-policy"
```

`X-Robots-Tag: noindex, nofollow, noarchive` が返れば、クロール対策が効いている。
**返らなければ `public/_headers` が `dist/` にコピーされていない** ので、そこを直す。

---

## 19. 次のアクション（承認待ち）

1. 本計画書のレビュー — 特に以下を確認いただきたい
   - **§7.2 ハイスコア画面の 3 ビュー** が意図に合っているか（今回の改訂の中心）
   - **§5.5 の追加種目でどれを採用するか**（推奨: ストループ・暗算・数唱）
   - **§14 のフェーズ分け**、特にフェーズ 3a（アカウント）の位置づけ
2. §18.1 の 3 点（Node / GitHub リポジトリ / Cloudflare アカウント）の準備
3. アプリ名の確定（現在 `neuroll` を仮採用）
4. 承認後、**フェーズ 0（`git init` + 疎通デプロイ + 計測基盤）から着手**

> 実装はまだ行わない。承認をもって着手する。
